# Core data stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Database / warehouse access
sqlalchemy>=2.0
psycopg2-binary>=2.9          # local Postgres (mimic-code build)
google-cloud-bigquery>=3.0    # BigQuery route (credentialed access)
pyarrow>=12.0                 # fast parquet / BigQuery transfers

# Modelling
lightgbm>=4.0                 # optional; pipeline falls back to logistic regression

# Viz
matplotlib>=3.7

# Dev / test
pytest>=7.0
